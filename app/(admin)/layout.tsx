/**
 * This file demonstrates how to properly set up the layout structure
 * Ensure all pages follow this pattern for consistency
 */

import { ReactNode } from "react";

export default function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
